namespace dotRAG.API.Application;

// Phase 0 stub — no logic.
// Phase 1: build prompt from system instructions + RAG chunks + user question.
internal sealed class PromptBuilder : IPromptBuilder
{
}
