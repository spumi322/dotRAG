namespace dotRAG.API.Infrastructure.RAG;

// Phase 0 stub — no logic.
// Phase 1: inject IEmbeddingService + InMemoryVectorStore;
// embed the question, run cosine similarity, return top-k NoteChunks.
internal sealed class NotesSearchService : Application.INotesSearchService
{
}
